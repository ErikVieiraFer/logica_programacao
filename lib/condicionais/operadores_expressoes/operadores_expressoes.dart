void main() {
// Operadores
  // ----------------------------------------------------------------------------------

  //verifica se o valor da esquerda é maior que ao da esquerda (2 > 1)
  // >= Verifica se o valor a esquerda é maior ou igual o da direita (2 >= 1) (2 >= 2)
  // < Verifica se o valor a esquerda é menor que o da direita (1 < 2)
  // <= Verifica se o valor a esqueda é menor ou igual o da direita (1 <= 2) (1 <= 1)
  // Igual(==) Verifica se o valor a esquerda é igual ao da direita (1 == 1)
  // Diferente(!=)  Verifica se o valor a esqueda é diferente do da direita (1 != 2)(2 != 1)

  // Expressão Relacional
}
